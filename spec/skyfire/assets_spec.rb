require 'spec_helper'

describe Npm::Assets do
  it 'has a version number' do
    expect(Npm::Assets::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
